"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const translator_1 = require("./translator");
describe('Translator defaults', () => {
    test('is available', () => {
        expect(translator_1.Translator.getError('default')).toBe('An unspecified error occurred');
    });
});
describe('getError', () => {
    test('falls back to default if key is not found', () => {
        expect(translator_1.Translator.getError('unknownKey')).toBe('An unspecified error occurred');
    });
    test("falls back to 'en-US' if locale not found", () => {
        translator_1.Translator.addError('test', 'Test');
        expect(translator_1.Translator.getError('test', 'af')).toBe('Test');
    });
    test('falls back to default if key and locale is not found', () => {
        expect(translator_1.Translator.getError('unknownKey', 'af')).toBe('An unspecified error occurred');
    });
});
describe('addError', () => {
    test('creates locale if it does not exist', () => {
        translator_1.Translator.addError('test', 'Toets %s', 'af');
        expect(translator_1.Translator.getError('test', 'af')).toBe('Toets %s');
    });
});
